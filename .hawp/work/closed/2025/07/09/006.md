# 006: Integration test - Build + run wails dev cycle

**Status:** done
**Created:** 2025-07-09
**Closed:** 2025-07-09 (backtracked — implemented before HAWP tracking was established)

---

## Context

Backend builds and frontend builds pass independently, but we haven't verified the full Wails dev cycle works end-to-end. This is a critical gate before any further work to ensure the app launches correctly.

## Scope

1. `go build ./...` from `src/` — should be green ✅
2. `npm run build` from `src/frontend/` — should pass ✅
3. `wails dev` launch cycle — verify app starts without panics
4. Check that profile search/settings UI actually renders (not just compiles)

## Files

- Terminal commands only (no code changes expected)

## Risks

- Wails runtime may have issues with new Store initialization
- Frontend Svelte reactivity bugs in production build mode

## Verification

- `wails dev` starts and shows UI within 30 seconds
- No console errors on launch
- Search input responds to typing
- Settings modal opens/closes cleanly
